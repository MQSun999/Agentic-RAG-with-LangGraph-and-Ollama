1.不使用get_latest_user_query这种方式获取user query，而是把当前流程的user query保存在state中，因为每个流程一定只会有一个user query
2.对于网络搜索，要限制llm使用tool的次数，否则llm可能会一直使用陷入很长时间的循环
3.对于这个组合Agent，如果用户的问题（比如：1+1=？），他自己就完全可以回答了，就不需要在走任何节点了，直接回答问题就可以
4.对于gpt-oss 20b模型，除了使用pydantic规定输出格式，还要在promp中说明输出格式，否则可能会出现因为模型太聪明而输出格式不对的问题